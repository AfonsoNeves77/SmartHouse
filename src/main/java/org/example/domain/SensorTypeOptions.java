package org.example.domain;

public enum SensorTypeOptions {
    /**
     * The following constants represent each Sensor Type Options.
     * They serve as a way to control the system's available Sensor Type.
     */
    HUMIDITY,TEMPERATURE,ON_OFF,OPEN_CLOSE;
}
